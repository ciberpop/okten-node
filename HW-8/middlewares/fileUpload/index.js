const isFileUploadValid = require('./isFileUploadValid.middleware');
const isPhotoCountValid = require('./isPhotoCountValid.middleware');

module.exports = {
    isFileUploadValid,
    isPhotoCountValid
}